---
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';
---

<Layout title="Agendar Demo - Kopecode">
  <section class="py-20 bg-gray-900">
    <div class="container mx-auto px-4">
      <div class="max-w-3xl mx-auto text-center">
        <h1 class="text-4xl md:text-5xl font-bold mb-6 bg-gradient-to-r from-blue-500 to-purple-600 bg-clip-text text-transparent">
          Agende uma Demonstração Gratuita!
        </h1>
        <p class="text-lg text-gray-300 mb-8">
          Descubra como a Kopecode pode ajudar a impulsionar seu negócio. Preencha o formulário abaixo para agendar uma demonstração personalizada.
        </p>

        <form action="/.netlify/functions/submit-demo-request" method="post" class="space-y-4"> {/* Replace with your form endpoint */}
          <input type="text" name="name" placeholder="Seu Nome" class="w-full px-4 py-3 rounded-lg bg-gray-700 text-white border border-gray-600 focus:border-blue-500 focus:outline-none" required>
          <input type="email" name="email" placeholder="Seu E-mail" class="w-full px-4 py-3 rounded-lg bg-gray-700 text-white border border-gray-600 focus:border-blue-500 focus:outline-none" required>
          <textarea name="message" placeholder="Sua Mensagem (opcional)" rows="4" class="w-full px-4 py-3 rounded-lg bg-gray-700 text-white border border-gray-600 focus:border-blue-500 focus:outline-none"></textarea>
          <Button type="submit" variant="primary">Enviar</Button>
        </form>
      </div>
    </div>
  </section>
</Layout>